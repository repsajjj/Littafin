<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.repsaj.littafin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/my_backup_rules"
        android:icon="@mipmap/ic_littafin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_littafin"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddBookActivity" />
        <activity android:name=".CategorieActivity" />
        <activity android:name=".barcode.BarcodeCaptureActivity" />
        <activity android:name=".EditBookActivity" />
        <activity android:name=".ViewBooksActivity"></activity>
    </application>

</manifest>